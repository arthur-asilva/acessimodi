import { StyleSheet } from "react-native";

const primaryColor = '#e1e1e1'

const main = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'column',
        backgroundColor: primaryColor
    }
});

export default main;